# Generated by @spaghetti_with_bazel//:extensions.bzl (the `spaghetti` extension).
# Edit spaghetti.config(...) in your MODULE.bazel, not this file.
load("@spaghetti_with_bazel//:config.bzl", "spaghetti_with_bazel_config")

spaghetti_with_bazel_config(
    name = "config",
    visibility = ["//visibility:public"],
    shared_areas = %{shared_areas},
    roots = %{roots},
    allowlist = %{allowlist},
    enabled_heuristics = %{enabled_heuristics},
    filename_rules = %{filename_rules},
)
